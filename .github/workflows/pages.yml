name: Deploy HTML reports to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "*.html"
      - "property_analysis/**"
      - "site/**"
      - "scripts/build_pages_site.py"
      - "sg_estate/reporting/**"
      - "models/gen_private_project_comparison_html.py"
      - "models/gen_project_exit_comparison_html.py"
      - "models/gen_condo_framework_comparison_html.py"
      - "models/gen_multi_condo_framework_comparison_html.py"
      - "models/multi_condo_transactions.py"
      - "models/private_project_catalog.py"
      - "models/project_identity_registry.py"
      - "data/inputs/ura_private.csv"
      - "data/outputs/private_project_locations.csv"
      - "data/outputs/private_project_school_metrics.csv"
      - "pyproject.toml"
      - "requirements-dev.txt"
      - "data/raw/edgeprop/edgeprop_condo_apartment_projects.csv"
      - "data/raw/edgeprop/edgeprop_condo_apartment_transactions_playwright_not_clean.csv"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install project
        run: |
          python -m pip install --requirement requirements-dev.txt
          python -m pip install --no-deps --editable .

      - name: Run required checks
        run: |
          python -m pytest -q
          python scripts/build_pages_site.py --out _site

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload validated Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
