# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Conversation logs
CONVERSATION-TRANSCRIPT.md

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Large raw downloads (reproducible via pipeline)
data/inputs/bus_services.json
data/inputs/bus_stops_raw.json

# Scraper scratch: attempt/error logs and probe dumps must never be committed
*_attempts.csv
*_retry_errors.csv
data/raw/edgeprop/probe/
data/**/*probe*/

# Browser authentication state contains reusable session credentials
*storage-state*.json
*storage_state*.json
playwright/.auth/

# Snapshot scratch (after_* are ephemeral; before/ is committed baseline)
tests/snapshots/after_*/

# Local GitHub Pages build artifact
_site/
_site-test/
_site-unowned-test/
